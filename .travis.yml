language: python
python:
  - "3.6"
  - "3.3"
 
# command to install dependencies
install:
  
  - pip install Mock
  - pip install requests
  - pip install pytest-cov
  
# command to run tests
script:
  - pytest
